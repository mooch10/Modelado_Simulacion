# Modelado_Simulacion
Scripts de modelado y simulacion
(Desarrollo del TPO)

librerias a intalar:

pip install streamlit numpy matplotlib sympy plotly
pip install pandas openpyxl

si te salta un error con pandas:
python -m pip install pandas openpyxl

para ejecutar el dashboard ejecutar el programa y escribir en la terminal:
streamlit run dashboard_integrador.py